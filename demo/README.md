# Demo — Orders API core

A deliberately small Node example used to practice the Stack. No external dependencies.

Run:
```bash
node --test test/*.test.js
```

Exercise: add pagination/filter behavior using `examples/orders-pagination/*` before looking at the sample solution notes.
