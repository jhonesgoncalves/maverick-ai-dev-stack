# Context Guide

Good context includes concrete paths, invariants, examples, commands and negative constraints. Bad context is a giant paste.

## Use this order
1. task goal;
2. boundary/entry point;
3. domain rule;
4. analogous implementation if trustworthy;
5. tests;
6. operational constraints.

Never elevate generated summaries above source code or current documentation.
