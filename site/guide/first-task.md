# First task

```bash
maverick init
maverick task add-orders-pagination
```

Fill `TASK.md`, then `CONTEXT.md`. Add a plan before implementation, and run:

```bash
maverick validate add-orders-pagination
maverick review add-orders-pagination
```

Give an agent the task, context, and plan—not unrestricted authority over the repository.
