# Configuration

`.maverick/config.json` contains a version, project metadata, workflow requirements, adapter name, task root, and required task/context sections. A legacy root `maverick.config.json` is read as a fallback.
