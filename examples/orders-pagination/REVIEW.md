# Example Review

This directory intentionally contains the desired review rubric, not a hidden "perfect answer". Implement the task in your own branch, then compare:
- did filtering occur before pagination?
- are zero and undefined handled differently?
- do invalid values fail clearly?
- did the diff stay inside two files?
- would a future reviewer understand why behavior is compatible?
